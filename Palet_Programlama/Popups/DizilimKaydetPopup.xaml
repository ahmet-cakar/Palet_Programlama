<Window x:Class="Palet_Programlama.UserController.MetinGirisKutusu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:language="clr-namespace:Palet_Programlama.Languages"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Height="600"
        Width="500"
        Background="Transparent">

  <Window.Resources>
    <language:LanguageConverter x:Key="LanguageConverter"/>
  </Window.Resources>

  <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
    <Border Background="#191919"
            BorderBrush="#f59f05"
            BorderThickness="2"
            CornerRadius="15"
            Padding="20">

      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtBaslik"
                   Text="{Binding Source='DizilimKaydetPopup.dizilimBilgisi',Converter={StaticResource LanguageConverter}}"
                   FontSize="24"
                   FontFamily="Roboto"
                   Foreground="White"
                   Margin="0,0,0,15"/>

        <TextBlock x:Name="txtMesaj"
                   Grid.Row="1"
                   Text="{Binding Source='DizilimKaydetPopup.degerGir',Converter={StaticResource LanguageConverter}}"
                   FontSize="16"
                   Foreground="#D0D0D0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <TextBox x:Name="txtGiris"
                 Grid.Row="2"
                 Height="50"
                 FontSize="22"
                 Padding="10"
                 Background="#2A2A2A"
                 Foreground="White"
                 BorderBrush="#f59f05"
                 BorderThickness="1"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,16"/>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
          <CheckBox x:Name="chkSeperatorKullanilacak"
                    Content="{Binding Source='DizilimKaydetPopup.SeperatorGirilecek',Converter={StaticResource LanguageConverter}}"
                    Foreground="White"
                    FontSize="16"
                    Checked="chkSeperatorKullanilacak_Checked"
                    Unchecked="chkSeperatorKullanilacak_Unchecked"/>
          <StackPanel x:Name="panelSeperatorKalinlik"
            Margin="0,10,0,10"
            Visibility="Collapsed">
            <TextBlock Text="{Binding Source='DizilimKaydetPopup.SeperatorKalingligi',Converter={StaticResource LanguageConverter}}"
               Foreground="White"
               FontSize="16"
               Margin="0,0,0,6"/>

            <TextBox x:Name="txtSeperatorKalinlik"
             Height="40"
             FontSize="18"
             Padding="10"
             Background="#2A2A2A"
             Foreground="White"
             BorderBrush="#f59f05"
             BorderThickness="1"
             VerticalContentAlignment="Center"
             PreviewTextInput="txtSeperatorKalinlik_PreviewTextInput"/>
          </StackPanel>

          <Border x:Name="borderSeperatorListe"
            Margin="0,12,0,0"
            Padding="12"
            Background="#2A2A2A"
            BorderBrush="#f59f05"
            BorderThickness="1"
            CornerRadius="10"
            Visibility="Visible"> 
            <StackPanel>
              <TextBlock Text="{Binding Source='DizilimKaydetPopup.SeperatorKonum',Converter={StaticResource LanguageConverter}}"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

              <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Height="120">
                <StackPanel x:Name="panelSeperatorListe"/>
              </ScrollViewer>
            </StackPanel>
          </Border>

        </StackPanel>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">

          <Button x:Name="btnTamam"
                  Content="{Binding Source='ButtonKey.btntamam',Converter={StaticResource LanguageConverter}}"
                  Width="150"
                  Height="40"
                  Margin="0,0,20,0"
                  Background="{StaticResource button_background}"
                  Style="{StaticResource button_template}"
                  Click="btnTamam_Click"/>

          <Button x:Name="btnIptal"
                  Content="{Binding Source='ButtonKey.btnIptal',Converter={StaticResource LanguageConverter}}"
                  Width="150"
                  Height="40"
                  Background="{StaticResource button_background}"
                  Style="{StaticResource button_template}"
                  Click="btnIptal_Click"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>