<Page x:Class="Palet_Programlama.Screens.Kullanici"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:language="clr-namespace:Palet_Programlama.Languages"
      xmlns:local="clr-namespace:Palet_Programlama.Screens"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1366"
      Title="Kullanici">
  <Page.Resources>
    <language:LanguageConverter  x:Key="LanguageConverter"/>
    <Style x:Key="BuyukCheckBoxStili" TargetType="CheckBox">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="18"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <Border Background="{TemplateBinding Background}"
                        CornerRadius="10"
                        Padding="{TemplateBinding Padding}">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="28"/>
                  <ColumnDefinition Width="10"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="Kutucuk"
                                Width="26"
                                Height="26"
                                CornerRadius="6"
                                BorderBrush="#D9FFFFFF"
                                BorderThickness="2"
                                Background="#22FFFFFF"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center">
                  <Path x:Name="Tik"
                                  Data="M 4 11 L 9 16 L 18 6"
                                  Stroke="White"
                                  StrokeThickness="2.5"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed"/>
                </Border>

                <ContentPresenter Grid.Column="2"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
              </Grid>
            </Border>

            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Kutucuk" Property="Background" Value="#3FAE5A"/>
                <Setter TargetName="Kutucuk" Property="BorderBrush" Value="#3FAE5A"/>
                <Setter TargetName="Tik" Property="Visibility" Value="Visible"/>
              </Trigger>

              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#18FFFFFF"/>
              </Trigger>

              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>


  </Page.Resources>
  <Grid>
        <Image Source="pack://application:,,,/Images/gradient_background.png" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="UniformToFill"/>
        <Grid>


      <ComboBox x:Name="cmbDil"
          Width="150"
          Height="50"
          Margin="0,50,30,0"
          SelectedIndex="0"
          HorizontalAlignment="Right"
          VerticalAlignment="Top"
          Background="Transparent"
          BorderThickness="0" SelectionChanged="cmbDil_SelectionChanged">

        <ComboBox.Style>
          <Style TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton Grid.Column="0"
              Focusable="False"
              ClickMode="Press"
              Background="Transparent"
              BorderBrush="Transparent"
              BorderThickness="0"
              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                      <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                          <Setter Property="Background" Value="Transparent"/>
                          <Setter Property="BorderBrush" Value="Transparent"/>
                          <Setter Property="BorderThickness" Value="0"/>
                          <Setter Property="Template">
                            <Setter.Value>
                              <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0">
                                  <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                              </ControlTemplate>
                            </Setter.Value>
                          </Setter>
                        </Style>
                      </ToggleButton.Style>

                      <ContentPresenter VerticalAlignment="Center"
                      HorizontalAlignment="Center"
                      Content="{TemplateBinding SelectionBoxItem}"
                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                      IsHitTestVisible="False"/>
                    </ToggleButton>

                    <ToggleButton Grid.Column="1"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                      <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                          <Setter Property="Background" Value="Transparent"/>
                          <Setter Property="BorderBrush" Value="Transparent"/>
                          <Setter Property="BorderThickness" Value="0"/>
                          <Setter Property="Template">
                            <Setter.Value>
                              <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent"
                                                            BorderBrush="Transparent"
                                                            BorderThickness="0">
                                  <Path Width="10"
                                                              Height="6"
                                                              Stretch="Fill"
                                                              Data="M 0 0 L 10 0 L 5 6 Z"
                                                              Fill="White"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                </Border>
                              </ControlTemplate>
                            </Setter.Value>
                          </Setter>
                        </Style>
                      </ToggleButton.Style>
                    </ToggleButton>

                    <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                      <Border Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        Padding="0">
                        <ScrollViewer Background="Transparent">
                          <StackPanel IsItemsHost="True"/>
                        </ScrollViewer>
                      </Border>
                    </Popup>
                  </Grid>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ComboBox.Style>

        <ComboBox.Resources>
          <Style TargetType="ComboBoxItem">
            <Setter Property="Height" Value="60"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                  <Border Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="0">
                    <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                  </Border>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </ComboBox.Resources>

        <ComboBoxItem>
          <Image Source="/Images/Kullanici/tr.png"
               Width="120"
               Height="50"
               Stretch="Fill"/>
        </ComboBoxItem>

        <ComboBoxItem>
          <Image Source="/Images/Kullanici/eng.png"
               Width="120"
               Height="50"
               Stretch="Fill"/>
        </ComboBoxItem>
      </ComboBox>


    </Grid>

        <StackPanel HorizontalAlignment="Center"  VerticalAlignment="Center" >
      <Image Source="pack://application:,,,/Images/Kullanici/logo-sade.png" Width="375" Margin="0,0,0,50"/>
            <!-- Kullanıcı Adı TextBox -->
            <Grid>
            <TextBox  x:Name="kullanici_textbox" Style="{StaticResource RoundedTextBox}" 
                     Tag="Kullanıcı Adı"
                     CaretBrush="White"
                     GotFocus="TextBox_GotFocus"
                     TextChanged="TextBox_TextChanged"
                      
                     LostFocus="TextBox_LostFocus"/>
                     
                <TextBlock x:Name="KullaniciPlaceholder"
                Text="Kullanıcı Adı"
                VerticalAlignment="Center"
                Margin="37,0,0,0"
                
                Foreground="White"
                IsHitTestVisible="False"
                
                FontSize="20"
                Opacity="0.4" Cursor="Help" />
            </Grid>
            <Grid>
            <!-- Şifre TextBox -->
            <PasswordBox x:Name="passwordBox" 
                         Tag="Şifre" 
                         CaretBrush="White"
                         
                         GotFocus="PasswordBox_GotFocus" 
                         LostFocus="PasswordBox_LostFocus"
                         PasswordChanged="PasswordBox_PasswordChanged"
                         Style="{StaticResource RoundedPasswordBox}" />
                <TextBlock x:Name="passwordPlaceholder"
                           Text="Şifre"
                           VerticalAlignment="Center"
                           Margin="37,0,0,0"
                           Foreground="White"
                           IsHitTestVisible="False"
                           FontSize="20"
                           Opacity="0.4" ForceCursor="True" >
                   
                </TextBlock>

            </Grid>
      <CheckBox x:Name="chkBeniHatirla"
          Style="{StaticResource BuyukCheckBoxStili}"
          Content="{Binding Source='Kullanici.beniHatirla',Converter={StaticResource LanguageConverter}}"
          Margin="0,15,0,0"
          HorizontalAlignment="Left"
          VerticalAlignment="Center"
          MinWidth="200"/>
      <Grid>
                <Button x:Name="btnGiris" Style="{StaticResource button_template}"
                        Background="{StaticResource button_background}" Content="Giriş" 
                        Width="170"  Height="40" Margin="0,20,100,0"
                        FontSize="20" Click="btnGiris_Click"/>
            </Grid>
         
        </StackPanel>
    </Grid>
</Page>
